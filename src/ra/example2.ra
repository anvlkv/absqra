declaredName: Sequence // with comment
	question outputName /* and multi line comment
						   closing like so */
		>text
content `some text` unexpectedVar
textVar = `some othe\`r text`
/[0-9][a-z].*^&?!Reg\\E\/**\/xp/ // btw it is invalid reg exp